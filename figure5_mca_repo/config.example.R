# Configuración local (NO subir a GitHub)
# Copia este archivo como 'config.R' y ajusta las rutas si lo necesitas.

# Carpeta raíz del proyecto (por defecto: directorio del script)
PROJECT_ROOT <- NULL

# Si no quieres usar here::here(), puedes definir rutas absolutas aquí:
DATA_DIR    <- NULL  # por defecto: "<repo>/data"
OUTPUTS_DIR <- NULL  # por defecto: "<repo>/outputs"
