# Table 1 – Baseline characteristics and clinical profile (clinical database; n=987)

## What this table shows
Table 1 summarizes demographic and clinical characteristics of candidiasis episodes from the **clinical database** (2014–2024), reporting **n (%)** for categorical variables and **median (IQR)** for continuous variables where applicable.

## Source workbook
- `Table1_PUBLIC.xlsx` *(rename to your actual workbook name if different)*

## Sheet/objects used
- Sheet: `Table1_final`
  - Contains the formatted table as included in the manuscript (variables, categories, n, %).
- (Optional) Sheet: `Table1_counts`
  - Aggregated counts used to build the final table (no individual-level records).

## How to reproduce the table (Excel)
1. Open `Table1_PUBLIC.xlsx`
2. If pivot tables/queries are used, go to **Data > Refresh All** (Datos > Actualizar todo)
3. Confirm that summary counts in `Table1_counts` (if present) are updated
4. The formatted table in `Table1_final` updates (or can be refreshed/recalculated)

## Export for journal submission (recommended)
- Export as **PDF** from Excel:
  - Select the Table 1 area > **File > Export > Create PDF/XPS**
  - Choose **Selection** (Selección) in Options
- Alternatively, copy the table into Word using **Paste Special > Keep Source Formatting** and ensure consistent decimals and totals.

## Data sharing note
For public sharing (e.g., GitHub/DOI), the workbook should contain **aggregated summaries only** and must exclude any individual-level identifiers or sensitive fields (e.g., patient IDs, names, dates of birth).
