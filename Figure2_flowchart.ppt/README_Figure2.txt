# Figure 2 – Distribution of the clinical spectrum of candidiasis (2014–2024)

## What this figure shows
This figure is a flowchart summarizing the distribution of candidiasis episodes (n=987) by recorded diagnosis (clinical database, 2014–2024), including mucocutaneous, invasive, and other/unspecified categories.

## Source file (editable)
- `Figure2_flowchart.pptx` (Microsoft PowerPoint)

## How to reproduce / edit (PowerPoint)
1. Open `Figure2_flowchart.pptx`
2. Edit text boxes (counts and labels) if needed
3. Ensure the layout fits a single slide (Panel/figure on one slide)

## Export for journal submission (recommended)
- Export as **PDF (vector)** from PowerPoint:
  - **File > Export > Create PDF/XPS**
  - Export the single slide containing Figure 2
- If a raster format is required, convert the exported PDF to **TIFF at 600 dpi**.

## Data sharing note
The diagram contains **aggregated counts only**. Do not include any individual-level identifiers (e.g., patient IDs, names, dates of birth) in the PowerPoint source file or in exported figures.
